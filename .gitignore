# Dépendances Node.js
node_modules/
backend/node_modules/
frontend/node_modules/

# Variables d'environnement (SECRETS !)
.env
.env.local
.env.production

# Build de production
frontend/dist/
backend/dist/

# Logs
*.log
npm-debug.log*

# Couverture de tests
coverage/
frontend/coverage/
backend/coverage/

# OS / IDE
.DS_Store
.vscode/
*.swp

# Docker (volumes locaux)
mysql_data/

# Artéfacts générés par tsc (compilation TypeScript de vite.config.ts)
# Ces fichiers sont auto-générés et ne doivent pas être versionnés
vite.config.js
vite.config.d.ts

# Cache de build incrémental TypeScript (généré par tsc --incremental)
# Recréé automatiquement à chaque build, inutile dans le dépôt
*.tsbuildinfo
database/init-clevercloud.sql
